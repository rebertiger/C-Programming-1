#include <stdio.h>
int main(void)
{
    int myAge = 19; // I am 19; let me see if I can drive
    if (myAge >= 16); {
    printf("I'm old enough to drive!");
    }
    if (myAge < 16) {
    printf("Not old enough yet... :*(");
    }
    return 0;
}
